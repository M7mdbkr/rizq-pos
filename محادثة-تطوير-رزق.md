# سجلّ تطوير نظام رزق POS — ملخص المحادثة الكاملة

> هذا ملف يحفظ كل قرارات وتفاصيل بناء نظام «رزق» حتى تقدر ترجع له من أي جهاز.
> آخر تحديث: يُحفظ داخل مجلد المشروع نفسه (`cashier/`) — انسخ المجلد كامل وهذا الملف ينتقل معه.

---

## ١) نظرة عامة على المشروع

**رزق (Rizq)** — نظام كاشير فاخر لمطعم سعودي، يعمل بالكامل داخل المتصفح بدون خادم (offline-first):
- **React 18 + TypeScript + Tailwind CSS**، قاعدة بيانات **SQLite عبر sql.js** محفوظة في **IndexedDB**.
- عربي بالكامل، RTL، وضع داكن/فاتح.
- تسجيل الدخول بـ PIN من ٨ أرقام، دورين: **مالك** و**كاشير**.

### بيانات الدخول التجريبية
| الدور | الرمز |
|---|---|
| المالك | `12345678` |
| كاشير | `11112222` / `33334444` |

---

## ٢) ترتيب زمني لما تم بناؤه

### المرحلة ١ — البناء الأساسي
- شاشة دخول بخطوتين: اختيار الدور (مالك/كاشير) ثم لوحة PIN.
- **واجهة الكاشير**: شبكة أصناف بالأقسام، سلة طلب، تعديل الأسطر (كمية/سعر/ملاحظة)، أصناف مخصصة، نوع الطلب (محلي/سفري/توصيل)، أكواد خصم، طرق دفع.
- **طباعة الفواتير**: فاتورة عميل + تذكرة مطبخ، دعم ورق حراري (58مم/80مم) وعدد نسخ، شعار مرفوع.
- **لوحة المالك**: Dashboard، Analytics، Inventory (المخزون)، Discounts، Reports (المحاسبة)، Settings.
- **قاعدة عملاء (CRM)**: تتجمّع تلقائيًا من الطلبات، شرائح تسويقية (كبار المنفقين، متكررون، غير نشطين…)، روابط واتساب جاهزة، تصدير Excel.
- **تصدير Excel متقدّم**: نافذة اختيار الفترة (اليوم/أمس/آخر٧/الشهر/مخصص) + اختيار الأوراق، باسم المطعم، وكل عملية تصدير تُسجَّل في قاعدة البيانات (جدول `report_exports`).

### المرحلة ٢ — تكاملات
- **تكامل تيليجرام**: بوت يرسل تقرير يومي (مبيعات، ربح، طرق دفع، الأكثر مبيعًا، مخزون ناقص) ويردّ على أسئلة المالك بنفس منطق المساعد.
- إصلاح **الوضع النهاري** (كانت الألوان غير واضحة) — أضيف متغيّر CSS للنص حسب الثيم + تجاوزات لكل الطبقات الشفافة.
- إصلاح تجاوب الشاشة الضيقة (آيباد عمودي) في الكاشير.

### المرحلة ٣ — مرونة كاملة + عمليات
- **طرق الدفع/التوصيل ديناميكية بالكامل**: جدول `payment_methods` في القاعدة، إضافة/تعديل/حذف/تفعيل أي عدد (كيتا، مرسول، نينجا، وأي اسم جديد)، أُزيل القيد القديم (CHECK constraint) من جدول `orders` عبر إعادة بناء الجدول بدون فقدان بيانات.
- **العمليات (الهدر + وجبات الموظفين)**: جدول `stock_consumption`، تسجيل هدر/تالف أو وجبة موظف يخصم من المخزون ويُحتسب كتكلفة، مع مؤشرات شهرية.

### المرحلة ٤ — المساعد الذكي الحقيقي (Claude API)
- تحويل «المساعد الذكي» من محرك قواعد بسيط إلى **ذكاء اصطناعي حقيقي** عبر `@anthropic-ai/sdk` يعمل من المتصفح مباشرة (CORS يسمح بذلك — مؤكَّد بالاختبار).
- **الموديل:** `claude-opus-4-8` مع Adaptive Thinking.
- **حلقة أدوات (tool-use loop)**: المساعد يملك أدوات فعلية تُنفَّذ على قاعدة البيانات الحقيقية:
  - قراءة: `get_dashboard`, `list_products`, `list_categories`, `list_payment_methods`, `sales_report`, `get_customers`.
  - كتابة: `update_product`, `restock_product`, `add_product`, `add_category`, `rename_category`, `create_discount`, `set_vat_rate`, `add_payment_method`, `rename_payment_method`, `set_business_info`, `log_consumption`.
- إعدادات: حقل مفتاح Claude API (يُحفظ محليًا على الجهاز) + زر اختبار الاتصال.
- عند عدم توفر مفتاح، يرجع تلقائيًا لمحرك القواعد المحلي (يشتغل أوفلاين).

### المرحلة ٥ — حماية البيانات
- **حفظ فوري عند إغلاق التطبيق** (`visibilitychange` / `pagehide`).
- **طلب تخزين دائم** من المتصفح (`navigator.storage.persist()`) لمنع الحذف التلقائي عند ضغط المساحة.
- **نسخ احتياطي كامل**: زر تنزيل نسخة (قاعدة البيانات + الإعدادات كملف JSON)، وزر استيراد لاسترجاعها أو نقلها لجهاز آخر. مُختبَر: تصدير/استيراد ٦٨٣ كيلوبايت بدون فقدان بيانات.

---

## ٣) كيف تشغّل النظام على أي جهاز

### النسخة الجاهزة (Zip)
كان يوجد ملف `rizq-pos.zip` جاهز (على سطح المكتب) يحتوي على نسخة `dist` مبنية مسبقًا + ملفات تشغيل:
- **ماك:** انقر مزدوج `start.command` (أول مرة: كليك يمين ← Open ← Open، أو شغّله من Terminal لو رفض).
- **ويندوز:** `start-windows.bat` (يحتاج Python أو Node).
- من جهاز ثانٍ (آيباد/جوال) على نفس شبكة الواي فاي: افتح `http://IP-الجهاز:4173`.

> ⚠️ ملاحظة: آخر نسخة zip كانت **قبل** إضافة الذكاء الاصطناعي والنسخ الاحتياطي — إذا احتجت أحدث نسخة اطلب إعادة البناء والتغليف.

### البناء من المصدر (يحتاج Node.js)
```bash
npm install
npm run build
npm run preview -- --host   # يعطي رابط محلي + رابط شبكة
```

### ملاحظة بيئة هذا الجهاز تحديدًا
جهاز التطوير الأصلي ما كان فيه Node.js مثبّت نظاميًا. استُخدم Node المرفق مع تطبيق **Kimi.app** للبناء والتشغيل (لأن Node المرفق مع Codex.app موقّع بحماية صارمة تمنع تحميل الإضافات الأصلية اللي يحتاجها Vite/Rolldown). لو ثبّت Node.js عادي (LTS) على أي جهاز، الأوامر القياسية تشتغل مباشرة بدون أي حل بديل.

---

## ٤) نقاط تقنية مهمة (لو تكمل التطوير لاحقًا)

- **قاعدة البيانات**: `src/db/database.ts` (تهيئة + هجرات + نسخ احتياطي)، `src/db/schema.ts` (تعريف الجداول)، `src/db/repo.ts` (كل عمليات القراءة/الكتابة)، `src/db/seed.ts` (بيانات تجريبية).
- **الإعدادات القابلة للتعديل**: `src/config/settings.ts` — اسم المتجر، الشعار، نسبة الضريبة، إعدادات الطباعة، مفاتيح تيليجرام وClaude.
- **المساعد الذكي**:
  - محرك القواعد المحلي: `src/screens/owner/assistant/engine.ts`.
  - الذكاء الحقيقي: `src/integrations/aiAssistant.ts`.
  - الواجهة: `src/screens/owner/Assistant.tsx`.
- **تيليجرام**: `src/integrations/telegram.ts`.
- **الوضع الفاتح**: تم إصلاحه عبر متغيّر CSS `--rz-fg` في `src/index.css` + تجاوزات `html.light .bg-white/…` إلخ.

---

## ٥) خطط مستقبلية نوقشت (لم تُبنَ بعد)

هذي أفكار اتفقنا نأجّلها، مذكورة هنا للرجوع لها لاحقًا:

1. **السحابة متعددة المطاعم (Supabase)** — قاعدة بيانات مشتركة، تسجيل دخول، تحديث لحظي (Realtime)، لإدارة أكثر من مطعم من حساب واحد ولتشغيل النظام ٢٤ ساعة حتى لو الجهاز مقفل.
2. **استقبال طلبات واتساب تلقائيًا** — عبر Meta WhatsApp Cloud API أو n8n، ينشئ الطلب ويطبع الفاتورة ويحدّث الشاشة لحظيًا (يحتاج المرحلة السحابية أولًا).
3. **طباعة تلقائية حقيقية بدون تدخل** — عبر وكيل طباعة على جهاز المطعم (PrintNode أو سكربت Node) لأن المتصفح يمنع الطباعة الصامتة لأسباب أمنية.
4. **ربط أوبسيدين (Obsidian)** — عبر File System Access API في المتصفح، يكتب تقرير يومي `.md` مباشرة في الـvault بدون خادم.
5. **ربط نوشن (Notion)** — عبر n8n Webhook (لأن نوشن يمنع الاتصال المباشر من المتصفح/CORS)، يرسل التقارير أو قاعدة العملاء كجدول.
6. **سجلّ المصروفات** (إيجار، رواتب، فواتير) لحساب **صافي ربح حقيقي** = المبيعات − التكلفة − الهدر − المصروفات.
7. **تطبيق Electron لويندوز** — نسخة `.exe` ذاتية التشغيل بدون الحاجة لـ Python/Node على جهاز العميل.

---

## ٦) كيف تكمل من هنا على جهاز جديد

1. انسخ **مجلد المشروع كامل** (`cashier/`) — هذا الملف بداخله.
2. افتح Claude Code داخل نفس المجلد، وابدأ بجملة مثل:
   > "اقرأ ملف `محادثة-تطوير-رزق.md` وكمّل معي من حيث توقفنا."
3. لو تبي تشغّل التطبيق فورًا بدل التطوير، ارجع لقسم «كيف تشغّل النظام» فوق.
