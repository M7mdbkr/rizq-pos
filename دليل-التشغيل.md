# دليل تشغيل وتحميل نظام رزق على أي جهاز

نظام **رزق** تطبيق ويب يعمل **محليًا وبدون إنترنت**. كل البيانات تُحفظ داخل متصفح الجهاز نفسه (IndexedDB).

---

## أولًا: المتطلبات (مرة واحدة على كل جهاز كمبيوتر)

1. **حمّل Node.js** (النسخة 20 أو أحدث) من الموقع الرسمي: <https://nodejs.org> ← اختر «LTS».
2. تأكد من التثبيت بفتح الطرفية (Terminal / PowerShell) وكتابة:
   ```bash
   node -v
   npm -v
   ```
   إذا ظهرت أرقام النسخة فالتثبيت تمام.

> ملاحظة لجهازك الحالي: لا يوجد Node مثبّت بشكل عام، لذلك استخدمنا Node المرفق مع تطبيق Kimi للبناء. على أي جهاز جديد، ثبّت Node.js الرسمي وكل شيء يعمل بالأوامر القياسية أدناه.

---

## ثانيًا: نسخ المشروع إلى الجهاز

انسخ مجلد المشروع كاملًا (`cashier`) إلى الجهاز الجديد — عبر USB، أو مجلد مشترك، أو Git. تأكد أنه يحتوي على ملف `package.json`.

---

## ثالثًا: التشغيل خطوة بخطوة (على جهاز واحد)

افتح الطرفية **داخل مجلد المشروع** ثم:

```bash
# 1) تثبيت المكتبات (مرة واحدة فقط)
npm install

# 2) للتجربة والتطوير
npm run dev
# يفتح على: http://localhost:5173
```

للاستخدام الفعلي (أسرع وأخف):

```bash
# 3) بناء النسخة النهائية
npm run build       # ينشئ مجلد dist

# 4) تشغيل النسخة النهائية
npm run preview -- --host
# يفتح على: http://localhost:4173
```

افتح الرابط في المتصفح (Chrome / Safari / Edge) وسجّل الدخول:

| الدور | الرمز |
|------|------|
| المالك | `12345678` |
| الكاشير | `11112222` |

---

## رابعًا: التشغيل على الآيباد أو جهاز ثانٍ عبر الشبكة المحلية

شغّل الخادم على جهاز الكمبيوتر الرئيسي مع `--host`، ثم افتحه من أي جهاز على **نفس شبكة الواي‑فاي**:

1. على الكمبيوتر الرئيسي:
   ```bash
   npm run preview -- --host
   ```
   ستظهر الطرفية عنوانًا مثل: `http://192.168.1.10:4173`
2. اعرف عنوان IP للكمبيوتر:
   - ويندوز: `ipconfig` ← ابحث عن IPv4
   - ماك: الإعدادات ← الشبكة، أو `ipconfig getifaddr en0`
3. على الآيباد/الجوال افتح المتصفح واكتب العنوان نفسه:
   `http://192.168.1.10:4173`

> **مهم بخصوص البيانات:** كل جهاز يحفظ بياناته داخل متصفحه الخاص. يعني إذا فتحت التطبيق على الآيباد والكمبيوتر، لكل واحد قاعدة بيانات منفصلة. للمزامنة الحقيقية بين الأجهزة (قاعدة بيانات واحدة مشتركة) تحتاج «المرحلة الثانية: المزامنة السحابية». حاليًا الأنسب: **جهاز كاشير واحد** هو المصدر، والباقي للعرض.

---

## خامسًا: الطباعة (متوافقة مع طابعات الفواتير الحرارية)

1. وصّل الطابعة الحرارية بالجهاز وثبّت تعريفها (58مم أو 80مم).
2. من **الإعدادات ← إعدادات الطباعة والفاتورة** اختر:
   - **حجم ورق الطابعة**: حراري 58مم / 80مم / عادي A4.
   - **عدد نسخ فاتورة العميل**: 1 إلى 5.
   - **طباعة تلقائية بعد تأكيد الطلب** (اختياري).
3. عند تأكيد أي طلب تظهر الفاتورة، اضغط **فاتورة العميل** أو **تذكرة المطبخ** → تفتح نافذة الطباعة، اختر الطابعة الحرارية واطبع.
4. لأفضل نتيجة في إعدادات الطباعة بالمتصفح: الهوامش = بلا (None)، والمقياس 100%.

---

## سادسًا: تصدير ملف Excel بكل تفاصيل المبيعات

من **المحاسبة والتقارير ← تصدير إلى Excel**. الملف يحتوي على أوراق:
**تفاصيل المبيعات** (كل صنف مُباع) • الطلبات • دفتر اليومية • المخزون • العملاء.

---

## سابعًا: تخصيص كل شيء

- **بيانات المتجر والشعار والضريبة**: الإعدادات ← بيانات المتجر والضريبة (يشمل رفع شعار الفاتورة).
- **أسماء تطبيقات التوصيل/الدفع** (كيتا، نينجا، مرسول…): الإعدادات ← أسماء طرق الدفع.
- **الأقسام والأصناف والأسعار**: المخزون (إضافة/تعديل/حذف).
- **المساعد الذكي**: نفّذ أي تعديل بالأوامر — مثل «غيّر اسم مرسول إلى هنقرستيشن» أو «أضف صنف عصير مانجو بسعر 18 في قسم المشروبات» أو «خلي الضريبة 15%».

---

## نسخ احتياطي / إعادة تهيئة

- البيانات داخل المتصفح؛ لا تمسح بيانات الموقع من إعدادات المتصفح وإلا تُفقد.
- لإعادة ضبط المصنع: الإعدادات ← منطقة الخطر ← إعادة تهيئة جميع البيانات.
